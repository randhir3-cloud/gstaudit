import React from 'react';
import PageHeader from '../../../components/common/PageHeader';

export default function InvestigationToolbar() {
  return (
    <PageHeader
      title="Audit Investigation Workbench"
      description="Review discrepancies, record findings, and mark case status."
      testId="investigation-page-header"
    />
  );
}
