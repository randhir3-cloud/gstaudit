/**
 * Central icon registry — import icons only through this module.
 * Wraps lucide-react for consistent sizing defaults.
 */
export {
  AlertCircle,
  AlertTriangle,
  ArrowDown,
  ArrowUp,
  CheckCircle2,
  Download,
  FileSpreadsheet,
  FileText,
  FileType,
  Files,
  HelpCircle,
  Loader2,
  Play,
  Sparkles,
  Trash2,
  Upload,
} from 'lucide-react';

export const iconSize = {
  xs: 'h-3 w-3',
  sm: 'h-4 w-4',
  md: 'h-5 w-5',
  lg: 'h-6 w-6',
};
