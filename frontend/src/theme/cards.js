import { radiusClass } from './radius';

export const cardBase = `${radiusClass.card} border border-border bg-card text-card-foreground`;
export const cardPadding = 'p-4 sm:p-5';
export const cardShell = `${cardBase} ${cardPadding}`;
